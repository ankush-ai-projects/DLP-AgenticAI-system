# Every dependency below was previously unpinned, meaning `pip install`
# could silently pull a different (possibly breaking, possibly vulnerable)
# version on every fresh install -- a real reproducibility and supply-chain
# risk in production. These pins reflect known-compatible versions as of
# this change; run `pip list --outdated` periodically and bump deliberately
# rather than leaving this file unpinned again.

# API and validation
fastapi==0.115.6
uvicorn[standard]==0.34.0
pydantic==2.12.5
pydantic-settings==2.12.0
email-validator==2.2.0
python-multipart==0.0.20

# Database and migrations
sqlalchemy==2.0.36
alembic==1.14.0
psycopg2-binary==2.9.10
pymysql==1.1.1
pyodbc==5.2.0

# Authentication
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
# Pinned below 4.1: passlib 1.7.4's backend-detection code calls a
# bcrypt internal API (`__about__.__version__`) that bcrypt>=4.1 removed,
# which breaks password hashing entirely at runtime (not at import time --
# it surfaces as a "password cannot be longer than 72 bytes" error on the
# first hash() call). Confirmed by reproducing it during testing.
bcrypt==4.0.1
slowapi==0.1.9

# Distributed execution and agent orchestration
celery==5.4.0
redis==5.2.1
langgraph==0.2.62
langgraph-checkpoint-sqlite==2.0.1
langgraph-checkpoint-postgres==2.0.9
psycopg[binary,pool]==3.2.3

# File processing
python-docx==1.1.2
openpyxl==3.1.5
pypdf==5.1.0
PyMuPDF==1.25.1
cryptography==49.0.0

# ML/NLP, evaluation, and observability
spacy==3.8.3
scikit-learn==1.6.0
mlflow==2.19.0
prometheus-client==0.21.1
opentelemetry-api==1.29.0
opentelemetry-sdk==1.29.0

# Testing and quality
pytest==8.3.4
pytest-asyncio==0.25.0
httpx==0.28.1
ruff==0.8.4

# MCP server (exposes read-only scan tools to Claude Desktop/Code and
# other MCP clients -- see mcp_server.py)
mcp>=2.0.0
